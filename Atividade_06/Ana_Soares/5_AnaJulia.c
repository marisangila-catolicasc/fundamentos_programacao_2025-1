#include <stdio.h>
 int main () {
  int numero, soma = 0;

  for (numero= 1; numero <= 100; numero++)
      soma += numero;
    {
    printf("A soma dos 100 primeiros numeros naturais sao: \n%d\n",soma);
      }
   return 0;
}
