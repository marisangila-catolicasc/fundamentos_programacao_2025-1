programa {
  funcao inicio() {
    inteiro entrada_user

    escreva("Digite um número inteiro: ")
    leia(entrada_user)

    limpa()
    escreva("O antecessor do número ", entrada_user, " é o ", entrada_user - 1, " e o sucessor do ", entrada_user, " é o ", entrada_user +1)
  }
}
