programa {
  funcao inicio() {
    inteiro numero, numero_2
    real resultado
    escreva("Digite dois números para a soma: ")
    leia(numero)
    leia(numero_2)
    resultado = numero + numero_2
    escreva("O resultado da soma é ", resultado)
  }
}
